{
  "info": {
    "name": "Algolia Ingestion API",
    "_postman_id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
    "description": "Read and write endpoints for the Algolia Ingestion (Connectors) API. Covers authentications, destinations, sources, tasks, and transformations.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "event": [
    {
      "listen": "prerequest",
      "script": {
        "type": "text/javascript",
        "exec": [
          "pm.request.headers.add({ key: 'X-Algolia-Application-Id', value: pm.environment.get('algoliaAppId') });",
          "pm.request.headers.add({ key: 'X-Algolia-API-Key', value: pm.environment.get('algoliaApiKey') });"
        ]
      }
    }
  ],
  "variable": [
    {
      "key": "baseUrl",
      "value": "https://data.us.algolia.com",
      "description": "Override via the active Postman environment. Change region as needed (us, eu)."
    }
  ],
  "item": [
    {
      "name": "Authentications",
      "item": [
        {
          "name": "List Authentications",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/1/authentications",
              "host": ["{{baseUrl}}"],
              "path": ["1", "authentications"],
              "query": [
                { "key": "itemsPerPage", "value": "20", "disabled": true, "description": "Number of items per page." },
                { "key": "page", "value": "1", "disabled": true, "description": "Page number (1-based)." },
                { "key": "type", "value": "basic", "disabled": true, "description": "Filter by authentication type. Repeat this key for multiple values." },
                { "key": "platform", "value": "bigcommerce", "disabled": true, "description": "Filter by platform. Repeat this key for multiple values." },
                { "key": "sort", "value": "createdAt", "disabled": true, "description": "Field to sort by." },
                { "key": "order", "value": "desc", "disabled": true, "description": "Sort direction: asc or desc." }
              ]
            },
            "description": "Retrieve a paginated list of authentications. Enable query params as needed."
          }
        },
        {
          "name": "Get Authentication",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/1/authentications/:authenticationID",
              "host": ["{{baseUrl}}"],
              "path": ["1", "authentications", ":authenticationID"],
              "variable": [
                {
                  "key": "authenticationID",
                  "value": "{{authenticationID}}",
                  "description": "Set authenticationID in your Postman environment."
                }
              ]
            },
            "description": "Retrieve a single authentication by ID. Set `authenticationID` in the active environment."
          }
        },
        {
          "name": "Create Authentication",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"type\": \"algolia\",\n  \"name\": \"My Algolia Auth\",\n  \"input\": {\n    \"appID\": \"{{algoliaAppId}}\",\n    \"apiKey\": \"{{algoliaApiKey}}\"\n  }\n}",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/1/authentications",
              "host": ["{{baseUrl}}"],
              "path": ["1", "authentications"]
            },
            "description": "Create a new authentication resource. Required fields: `type`, `name`, `input`. Optional fields: `platform`. The `input` shape varies by `type` (e.g. algolia, basic, oauth, googleServiceAccount)."
          }
        },
        {
          "name": "Update Authentication",
          "request": {
            "method": "PATCH",
            "header": [
              { "key": "Content-Type", "value": "application/json" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Updated Auth Name\"\n}",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/1/authentications/:authenticationID",
              "host": ["{{baseUrl}}"],
              "path": ["1", "authentications", ":authenticationID"],
              "variable": [
                {
                  "key": "authenticationID",
                  "value": "{{authenticationID}}",
                  "description": "Set authenticationID in your Postman environment."
                }
              ]
            },
            "description": "Partially update an authentication resource (PATCH). All fields are optional — only send fields you want to change. Updatable fields: `name`, `input`."
          }
        },
        {
          "name": "Delete Authentication",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/1/authentications/:authenticationID",
              "host": ["{{baseUrl}}"],
              "path": ["1", "authentications", ":authenticationID"],
              "variable": [
                {
                  "key": "authenticationID",
                  "value": "{{authenticationID}}",
                  "description": "Set authenticationID in your Postman environment."
                }
              ]
            },
            "description": "Delete an authentication resource by ID. This is irreversible. Set `authenticationID` in the active environment."
          }
        }
      ]
    },
    {
      "name": "Destinations",
      "item": [
        {
          "name": "List Destinations",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/1/destinations",
              "host": ["{{baseUrl}}"],
              "path": ["1", "destinations"],
              "query": [
                { "key": "itemsPerPage", "value": "20", "disabled": true, "description": "Number of items per page." },
                { "key": "page", "value": "1", "disabled": true, "description": "Page number (1-based)." },
                { "key": "type", "value": "search", "disabled": true, "description": "Filter by destination type. Repeat this key for multiple values." },
                { "key": "authenticationID", "value": "", "disabled": true, "description": "Filter by authentication ID. Repeat this key for multiple values." },
                { "key": "transformationID", "value": "", "disabled": true, "description": "Filter by transformation ID. Repeat this key for multiple values." },
                { "key": "sort", "value": "createdAt", "disabled": true, "description": "Field to sort by." },
                { "key": "order", "value": "desc", "disabled": true, "description": "Sort direction: asc or desc." }
              ]
            },
            "description": "Retrieve a paginated list of destinations. Enable query params as needed."
          }
        },
        {
          "name": "Get Destination",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/1/destinations/:destinationID",
              "host": ["{{baseUrl}}"],
              "path": ["1", "destinations", ":destinationID"],
              "variable": [
                {
                  "key": "destinationID",
                  "value": "{{destinationID}}",
                  "description": "Set destinationID in your Postman environment."
                }
              ]
            },
            "description": "Retrieve a single destination by ID. Set `destinationID` in the active environment."
          }
        },
        {
          "name": "Create Destination",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"type\": \"search\",\n  \"name\": \"My Destination\",\n  \"input\": {\n    \"indexName\": \"my_index\"\n  },\n  \"authenticationID\": \"{{authenticationID}}\"\n}",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/1/destinations",
              "host": ["{{baseUrl}}"],
              "path": ["1", "destinations"]
            },
            "description": "Create a new destination resource. Required fields: `type`, `name`, `input`. Optional fields: `authenticationID`, `transformationIDs` (array of transformation UUIDs)."
          }
        },
        {
          "name": "Update Destination",
          "request": {
            "method": "PATCH",
            "header": [
              { "key": "Content-Type", "value": "application/json" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Updated Destination Name\"\n}",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/1/destinations/:destinationID",
              "host": ["{{baseUrl}}"],
              "path": ["1", "destinations", ":destinationID"],
              "variable": [
                {
                  "key": "destinationID",
                  "value": "{{destinationID}}",
                  "description": "Set destinationID in your Postman environment."
                }
              ]
            },
            "description": "Partially update a destination resource (PATCH). All fields are optional. Updatable fields: `type`, `name`, `input`, `authenticationID`, `transformationIDs`."
          }
        },
        {
          "name": "Delete Destination",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/1/destinations/:destinationID",
              "host": ["{{baseUrl}}"],
              "path": ["1", "destinations", ":destinationID"],
              "variable": [
                {
                  "key": "destinationID",
                  "value": "{{destinationID}}",
                  "description": "Set destinationID in your Postman environment."
                }
              ]
            },
            "description": "Delete a destination resource by ID. This is irreversible. Set `destinationID` in the active environment."
          }
        }
      ]
    },
    {
      "name": "Sources",
      "item": [
        {
          "name": "List Sources",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/1/sources",
              "host": ["{{baseUrl}}"],
              "path": ["1", "sources"],
              "query": [
                { "key": "itemsPerPage", "value": "20", "disabled": true, "description": "Number of items per page." },
                { "key": "page", "value": "1", "disabled": true, "description": "Page number (1-based)." },
                { "key": "type", "value": "commercetools", "disabled": true, "description": "Filter by source type. Repeat this key for multiple values." },
                { "key": "authenticationID", "value": "", "disabled": true, "description": "Filter by authentication ID. Repeat this key for multiple values." },
                { "key": "sort", "value": "createdAt", "disabled": true, "description": "Field to sort by." },
                { "key": "order", "value": "desc", "disabled": true, "description": "Sort direction: asc or desc." }
              ]
            },
            "description": "Retrieve a paginated list of sources. Enable query params as needed."
          }
        },
        {
          "name": "Get Source",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/1/sources/:sourceID",
              "host": ["{{baseUrl}}"],
              "path": ["1", "sources", ":sourceID"],
              "variable": [
                {
                  "key": "sourceID",
                  "value": "{{sourceID}}",
                  "description": "Set sourceID in your Postman environment."
                }
              ]
            },
            "description": "Retrieve a single source by ID. Set `sourceID` in the active environment."
          }
        },
        {
          "name": "Create Source",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"type\": \"push\",\n  \"name\": \"My Push Source\"\n}",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/1/sources",
              "host": ["{{baseUrl}}"],
              "path": ["1", "sources"]
            },
            "description": "Create a new source resource. Required fields: `type`, `name`. Optional fields: `input` (source-type specific config), `authenticationID`."
          }
        },
        {
          "name": "Update Source",
          "request": {
            "method": "PATCH",
            "header": [
              { "key": "Content-Type", "value": "application/json" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Updated Source Name\"\n}",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/1/sources/:sourceID",
              "host": ["{{baseUrl}}"],
              "path": ["1", "sources", ":sourceID"],
              "variable": [
                {
                  "key": "sourceID",
                  "value": "{{sourceID}}",
                  "description": "Set sourceID in your Postman environment."
                }
              ]
            },
            "description": "Partially update a source resource (PATCH). All fields are optional. Updatable fields: `name`, `input`, `authenticationID`."
          }
        },
        {
          "name": "Delete Source",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/1/sources/:sourceID",
              "host": ["{{baseUrl}}"],
              "path": ["1", "sources", ":sourceID"],
              "variable": [
                {
                  "key": "sourceID",
                  "value": "{{sourceID}}",
                  "description": "Set sourceID in your Postman environment."
                }
              ]
            },
            "description": "Delete a source resource by ID. This is irreversible. Set `sourceID` in the active environment."
          }
        }
      ]
    },
    {
      "name": "Tasks",
      "item": [
        {
          "name": "List Tasks",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/2/tasks",
              "host": ["{{baseUrl}}"],
              "path": ["2", "tasks"],
              "query": [
                { "key": "itemsPerPage", "value": "20", "disabled": true, "description": "Number of items per page." },
                { "key": "page", "value": "1", "disabled": true, "description": "Page number (1-based)." },
                { "key": "action", "value": "replace", "disabled": true, "description": "Filter by action type. Repeat this key for multiple values." },
                { "key": "enabled", "value": "true", "disabled": true, "description": "Filter by enabled status." },
                { "key": "sourceID", "value": "", "disabled": true, "description": "Filter by source ID. Repeat this key for multiple values." },
                { "key": "sourceType", "value": "commercetools", "disabled": true, "description": "Filter by source type. Repeat this key for multiple values." },
                { "key": "destinationID", "value": "", "disabled": true, "description": "Filter by destination ID. Repeat this key for multiple values." },
                { "key": "triggerType", "value": "onDemand", "disabled": true, "description": "Filter by trigger type. Repeat this key for multiple values." },
                { "key": "withEmailNotifications", "value": "false", "disabled": true, "description": "Filter tasks that have email notifications configured." },
                { "key": "sort", "value": "createdAt", "disabled": true, "description": "Field to sort by." },
                { "key": "order", "value": "desc", "disabled": true, "description": "Sort direction: asc or desc." }
              ]
            },
            "description": "Retrieve a paginated list of tasks. Note: tasks use API version /2/. Enable query params as needed."
          }
        },
        {
          "name": "Get Task",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/2/tasks/:taskID",
              "host": ["{{baseUrl}}"],
              "path": ["2", "tasks", ":taskID"],
              "variable": [
                {
                  "key": "taskID",
                  "value": "{{taskID}}",
                  "description": "Set taskID in your Postman environment."
                }
              ]
            },
            "description": "Retrieve a single task by ID. Note: tasks use API version /2/. Set `taskID` in the active environment."
          }
        },
        {
          "name": "Create Task",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"sourceID\": \"{{sourceID}}\",\n  \"destinationID\": \"{{destinationID}}\",\n  \"action\": \"replace\",\n  \"enabled\": true\n}",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/2/tasks",
              "host": ["{{baseUrl}}"],
              "path": ["2", "tasks"]
            },
            "description": "Create a new task (API v2). Required fields: `sourceID`, `destinationID`, `action`. Optional fields: `cron` (schedule expression), `enabled`, `failureThreshold`, `input`, `cursor` (RFC 3339), `notifications`, `policies`, `subscriptionAction`."
          }
        },
        {
          "name": "Update Task",
          "request": {
            "method": "PATCH",
            "header": [
              { "key": "Content-Type", "value": "application/json" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"enabled\": false\n}",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/2/tasks/:taskID",
              "host": ["{{baseUrl}}"],
              "path": ["2", "tasks", ":taskID"],
              "variable": [
                {
                  "key": "taskID",
                  "value": "{{taskID}}",
                  "description": "Set taskID in your Postman environment."
                }
              ]
            },
            "description": "Partially update a task (PATCH, API v2). All fields are optional. Updatable fields: `destinationID`, `cron`, `input`, `enabled`, `failureThreshold`, `notifications`, `policies`, `subscriptionAction`."
          }
        },
        {
          "name": "Replace Task",
          "request": {
            "method": "PUT",
            "header": [
              { "key": "Content-Type", "value": "application/json" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"destinationID\": \"{{destinationID}}\",\n  \"action\": \"replace\",\n  \"enabled\": true\n}",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/2/tasks/:taskID",
              "host": ["{{baseUrl}}"],
              "path": ["2", "tasks", ":taskID"],
              "variable": [
                {
                  "key": "taskID",
                  "value": "{{taskID}}",
                  "description": "Set taskID in your Postman environment."
                }
              ]
            },
            "description": "Fully replace a task (PUT, API v2). Required fields: `destinationID`, `action`. Optional fields: `cron`, `enabled`, `failureThreshold`, `input`, `cursor` (RFC 3339), `notifications`, `policies`, `subscriptionAction`."
          }
        },
        {
          "name": "Delete Task",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/2/tasks/:taskID",
              "host": ["{{baseUrl}}"],
              "path": ["2", "tasks", ":taskID"],
              "variable": [
                {
                  "key": "taskID",
                  "value": "{{taskID}}",
                  "description": "Set taskID in your Postman environment."
                }
              ]
            },
            "description": "Delete a task by ID (API v2). This is irreversible. Set `taskID` in the active environment."
          }
        }
      ]
    },
    {
      "name": "Observability",
      "item": [
        {
          "name": "List Runs",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/1/runs",
              "host": ["{{baseUrl}}"],
              "path": ["1", "runs"],
              "query": [
                { "key": "itemsPerPage", "value": "20", "disabled": true, "description": "Number of items per page." },
                { "key": "page", "value": "1", "disabled": true, "description": "Page number (1-based)." },
                { "key": "status", "value": "finished", "disabled": true, "description": "Filter by run status (created, started, finished, skipped). Repeat this key for multiple values." },
                { "key": "taskID", "value": "", "disabled": true, "description": "Filter by task ID. Repeat this key for multiple values." },
                { "key": "sort", "value": "createdAt", "disabled": true, "description": "Field to sort by." },
                { "key": "order", "value": "desc", "disabled": true, "description": "Sort direction: asc or desc." },
                { "key": "startDate", "value": "", "disabled": true, "description": "Filter runs created on or after this date (RFC 3339)." },
                { "key": "endDate", "value": "", "disabled": true, "description": "Filter runs created on or before this date (RFC 3339)." }
              ]
            },
            "description": "Retrieve a paginated list of task runs. Enable query params as needed. Use `status` to filter by run outcome and `taskID` to scope to a specific task."
          }
        },
        {
          "name": "Get Run",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/1/runs/:runID",
              "host": ["{{baseUrl}}"],
              "path": ["1", "runs", ":runID"],
              "variable": [
                {
                  "key": "runID",
                  "value": "{{runID}}",
                  "description": "Set runID in your Postman environment."
                }
              ]
            },
            "description": "Retrieve a single task run by ID. Set `runID` in the active environment."
          }
        },
        {
          "name": "List Events",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/1/runs/:runID/events",
              "host": ["{{baseUrl}}"],
              "path": ["1", "runs", ":runID", "events"],
              "variable": [
                {
                  "key": "runID",
                  "value": "{{runID}}",
                  "description": "Set runID in your Postman environment."
                }
              ],
              "query": [
                { "key": "itemsPerPage", "value": "20", "disabled": true, "description": "Number of items per page." },
                { "key": "page", "value": "1", "disabled": true, "description": "Page number (1-based)." },
                { "key": "status", "value": "created", "disabled": true, "description": "Filter by event status (created, started, retried, failed, succeeded, critical). Repeat this key for multiple values." },
                { "key": "type", "value": "fetch", "disabled": true, "description": "Filter by event type (fetch, push, transform, validate). Repeat this key for multiple values." },
                { "key": "sort", "value": "createdAt", "disabled": true, "description": "Field to sort by." },
                { "key": "order", "value": "desc", "disabled": true, "description": "Sort direction: asc or desc." },
                { "key": "startDate", "value": "", "disabled": true, "description": "Filter events created on or after this date (RFC 3339)." },
                { "key": "endDate", "value": "", "disabled": true, "description": "Filter events created on or before this date (RFC 3339)." }
              ]
            },
            "description": "Retrieve a paginated list of events for a specific task run. Set `runID` in the active environment. Enable query params as needed. Use `status` and `type` to narrow results."
          }
        },
        {
          "name": "Get Event",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/1/runs/:runID/events/:eventID",
              "host": ["{{baseUrl}}"],
              "path": ["1", "runs", ":runID", "events", ":eventID"],
              "variable": [
                {
                  "key": "runID",
                  "value": "{{runID}}",
                  "description": "Set runID in your Postman environment."
                },
                {
                  "key": "eventID",
                  "value": "{{eventID}}",
                  "description": "Set eventID in your Postman environment."
                }
              ]
            },
            "description": "Retrieve a single event within a task run by its ID. Set both `runID` and `eventID` in the active environment."
          }
        }
      ]
    },
    {
      "name": "Transformations",
      "item": [
        {
          "name": "List Transformations",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/1/transformations",
              "host": ["{{baseUrl}}"],
              "path": ["1", "transformations"],
              "query": [
                { "key": "itemsPerPage", "value": "20", "disabled": true, "description": "Number of items per page." },
                { "key": "page", "value": "1", "disabled": true, "description": "Page number (1-based)." },
                { "key": "type", "value": "code", "disabled": true, "description": "Filter by transformation type. Repeat this key for multiple values." },
                { "key": "sort", "value": "createdAt", "disabled": true, "description": "Field to sort by." },
                { "key": "order", "value": "desc", "disabled": true, "description": "Sort direction: asc or desc." }
              ]
            },
            "description": "Retrieve a paginated list of transformations. Enable query params as needed."
          }
        },
        {
          "name": "Get Transformation",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/1/transformations/:transformationID",
              "host": ["{{baseUrl}}"],
              "path": ["1", "transformations", ":transformationID"],
              "variable": [
                {
                  "key": "transformationID",
                  "value": "{{transformationID}}",
                  "description": "Set transformationID in your Postman environment."
                }
              ]
            },
            "description": "Retrieve a single transformation by ID. Set `transformationID` in the active environment."
          }
        },
        {
          "name": "Create Transformation",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"My Transformation\",\n  \"type\": \"code\",\n  \"input\": {\n    \"code\": \"// transformation code here\"\n  },\n  \"description\": \"Description of what this transformation does\"\n}",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/1/transformations",
              "host": ["{{baseUrl}}"],
              "path": ["1", "transformations"]
            },
            "description": "Create a new transformation resource. Required field: `name`. Optional fields: `type`, `input` (contains `code`), `description`, `authenticationIDs` (array of authentication UUIDs). Note: the `code` top-level field is deprecated — use `input.code` instead."
          }
        },
        {
          "name": "Update Transformation",
          "request": {
            "method": "PUT",
            "header": [
              { "key": "Content-Type", "value": "application/json" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"My Transformation\",\n  \"type\": \"code\",\n  \"input\": {\n    \"code\": \"// updated transformation code here\"\n  },\n  \"description\": \"Updated description\"\n}",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/1/transformations/:transformationID",
              "host": ["{{baseUrl}}"],
              "path": ["1", "transformations", ":transformationID"],
              "variable": [
                {
                  "key": "transformationID",
                  "value": "{{transformationID}}",
                  "description": "Set transformationID in your Postman environment."
                }
              ]
            },
            "description": "Fully replace a transformation (PUT). This is a full replacement — include all fields you want to keep. Same field shape as Create Transformation. Set `transformationID` in the active environment."
          }
        },
        {
          "name": "Delete Transformation",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/1/transformations/:transformationID",
              "host": ["{{baseUrl}}"],
              "path": ["1", "transformations", ":transformationID"],
              "variable": [
                {
                  "key": "transformationID",
                  "value": "{{transformationID}}",
                  "description": "Set transformationID in your Postman environment."
                }
              ]
            },
            "description": "Delete a transformation resource by ID. This is irreversible. Set `transformationID` in the active environment."
          }
        }
      ]
    }
  ]
}
